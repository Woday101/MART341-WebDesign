Browsers function by presenting the web resource of your choice by requesting it from the server that will display it on your browser window using HTML files and CSS. I use Google Chrom and Safari as my web browsers.

A markup language is a system for annotating a document in a way that is visually distingusitable that is used to format the text so that the markup language does not appear. One commonly used is HTMl or XML.


[My Responses](.responses.txt)
![screenshot](./images/screenshot.png)
